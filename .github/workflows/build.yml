name: Build Kernel

on:
  push:
    branches:
      - umi-new-aptusitu17
  workflow_dispatch:
    inputs:
      device:
        description: 'Target Device Code (设备代号)'
        required: true
        default: 'umi'
        type: choice
        options:
          - umi
          - munch
      ksu:
        description: 'Enable KernelSU (ReSukiSU)'
        required: true
        type: boolean
        default: true
      target_os:
        description: 'Target OS (目标系统)'
        required: true
        default: 'miui'
        type: choice
        options:
          - aosp
          - miui
          - both
      release:
        description: 'Publish to GitHub Releases (发布到 Releases)'
        required: true
        type: boolean
        default: true

  schedule:
    - cron: '13 12 * * 1-6'
    - cron: '59 14 * * 0'

env:
  CCACHE_DIR: ~/.cache/ccache_mikernel
  CCACHE_MAXSIZE: 10G
  CCACHE_COMPRESS: 1
  CCACHE_COMPRESSLEVEL: 6

jobs:
  build-kernel:
    name: Build ${{ github.event.inputs.device || 'umi' }} | KSU:${{ github.event.inputs.ksu || true }} | OS:${{ github.event.inputs.target_os || 'miui' }}
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Set up Environment
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends \
            bc bison build-essential ccache curl flex gawk git gnupg gperf \
            imagemagick libncurses-dev lib32z1-dev liblz4-tool \
            libsdl1.2-compat-dev libssl-dev libxml2-utils lzop pngcrush rsync \
            schedtool squashfs-tools xsltproc zip zlib1g-dev python3 \
            dwarves libelf-dev

      - name: Cache zyc-clang Toolchain
        id: cache-clang
        uses: actions/cache@v4
        with:
          path: ~/zyc-clang
          key: zyc-clang-16.0.6-20260510

      - name: Setup zyc-clang 16 Toolchain
        if: steps.cache-clang.outputs.cache-hit != 'true'
        run: |
          mkdir -p $HOME/zyc-clang
          cd $HOME/zyc-clang
          wget -q https://github.com/ZyCromerZ/Clang/releases/download/16.0.6-20260510-release/Clang-16.0.6-20260510.tar.gz
          tar -zxvf Clang-16.0.6-20260510.tar.gz
          rm Clang-16.0.6-20260510.tar.gz

      - name: Cache ccache
        uses: actions/cache@v4
        with:
          path: ${{ env.CCACHE_DIR }}
          key: ${{ runner.os }}-kernel-ccache-${{ github.event.inputs.device || 'umi' }}-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-kernel-ccache-${{ github.event.inputs.device || 'umi' }}-
            ${{ runner.os }}-kernel-ccache-

      - name: Configure ccache & Print Pre-Build Stats
        run: |
          ccache -M ${{ env.CCACHE_MAXSIZE }}
          ccache -z
          echo "=== Pre-Build Ccache Stats ==="
          ccache -s

      - name: Modify kernel version string (Remove cip/st extra suffix)
        run: |
          sed -i 's|^EXTRAVERSION =.*|EXTRAVERSION = |' Makefile
            
          find arch/arm64/configs -maxdepth 1 -type f -name "*defconfig" | while read -r file; do
            sed -i '/CONFIG_LOCALVERSION_AUTO/d' "$file"
            echo 'CONFIG_LOCALVERSION_AUTO=n' >> "$file"
            sed -i '/CONFIG_LOCALVERSION=/d' "$file"
            echo 'CONFIG_LOCALVERSION=""' >> "$file"
          done
            
          echo '#!/bin/sh' > scripts/setlocalversion
          echo 'echo ""' >> scripts/setlocalversion
          chmod +x scripts/setlocalversion

      - name: Run Build Script
        run: |
          chmod +x build_kernel.sh
          export LOCALVERSION=""
          export USE_CCACHE=1
          export CCACHE_EXEC=$(which ccache)
            
          DEVICE="${{ github.event.inputs.device || 'umi' }}"
          KSU="${{ github.event.inputs.ksu || 'true' }}"
          TARGET_OS="${{ github.event.inputs.target_os || 'miui' }}"
            
          ARGS="$DEVICE"
          [ "$KSU" == "true" ] && ARGS="$ARGS ksu"
          [ "$TARGET_OS" != "both" ] && ARGS="$ARGS $TARGET_OS"
            
          echo "Executing command: ./build_kernel.sh $ARGS"
          ./build_kernel.sh $ARGS

      - name: Print Post-Build Ccache Stats
        if: always()
        run: |
          echo "=== Post-Build Ccache Stats ==="
          ccache -s

      - name: Upload Flashable ZIP (Artifact)
        uses: actions/upload-artifact@v4
        with:
          name: DEBUG_APTKernel-${{ github.event.inputs.device || 'umi' }}-${{ github.event.inputs.target_os || 'miui' }}-${{ github.run_id }}
          path: "*APTKernel_*.zip"
          compression-level: 0

      - name: Publish Release
        if: ${{ github.event_name == 'schedule' || github.event_name == 'push' || github.event.inputs.release != 'false' }}
        uses: softprops/action-gh-release@v2
        with:
          files: "*APTKernel_*.zip"
          tag_name: kernel-${{ github.event.inputs.device || 'umi' }}-${{ github.run_id }}
          name: "Kernel Build for ${{ github.event.inputs.device || 'umi' }} (${{ github.event.inputs.target_os || 'miui' }})"
          body: |
            Automatic kernel build.
            - **Device:** ${{ github.event.inputs.device || 'umi' }}
            - **KernelSU:** ${{ github.event.inputs.ksu || 'true' }}
            - **Target OS:** ${{ github.event.inputs.target_os || 'miui' }}
          draft: false
          prerelease: false
